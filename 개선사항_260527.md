# 개선사항 정리 (2026-05-27)

> 작성일: 2026-05-27
> 목적: 엑셀 업로드 실패 및 RAG 검색 오답 문제 해결

---

## 1. 고객사 구분이 안 된 이유

### 현상

업로드된 모든 문서가 `customer_id=1` (K금융)으로 저장됨.

### 원인

프론트엔드 `DocumentPage.tsx:28`에서 고객사 ID가 하드코딩되어 있음:

```typescript
const [customerId] = useState(1)  // 항상 1번 고객사
```

고객사 선택 드롭다운 UI가 구현되지 않았기 때문에, 사용자가 어떤 문서를 올리든 모두 K금융 소속으로 저장됨.

### 추가 문제

`05_통합서버현황_80대_불규칙.xlsx`처럼 하나의 엑셀 파일에 A금융/B카드/C보험/D증권/E캐피탈 등 여러 고객사가 혼재된 경우, 고객사 선택 UI를 추가하더라도 파일 단위 구분만으로는 행 단위 고객사 식별이 불가능함.

---

## 2. E캐피탈 서버 개수 질문에 오답이 나온 이유

### 현상

"E캐피탈 서버 몇 대?" 질문에 LLM이 **3대**라고 답변. 실제로는 **16대**.

### 원인: 3중 데이터 손실

E캐피탈 서버 데이터가 LLM에 도달하기까지 3단계에서 잘려나감:

```
엑셀 원본: E캐피탈 서버 16대
    ↓
[1단계: 청킹] 1000자 고정 슬라이딩 윈도우 (config.py:59)
    → 고객사 구분 없이 순서대로 자름
    → E캐피탈 서버가 16개 청크 중 12개에 1~4대씩 분산
    ↓
[2단계: 검색] top-20 반환 (schemas/search.py:12)
    → 12개 청크 모두 E캐피탈을 포함하지만 다른 고객사와 섞여 있어 벡터 유사도 희석
    → 상위 20개에 E캐피탈 포함 청크 약 10개 도달
    ↓
[3단계: LLM 전달] 3중 잘림
    → generate_response(): results[:3] (agentic_rag.py:234) → 3개만 사용
    → generate_response(): content[:200] (agentic_rag.py:239) → 200자로 잘림
    → _format_single_pass_response(): results[:5] (agentic_rag.py:316) → 5개만 사용
    ↓
LLM이 보는 E캐피탈 서버: 3대
```

---

## 3. 오답이 나오도록 설계된 이유

### 의도적 설계 — 비용/속도 최적화

각 제한은 LLM 토큰 비용과 응답 속도를 절감하기 위해 의도적으로 설정된 값:

| 제한 | 목적 |
|------|------|
| 1000자 청킹 | 임베딩 벡터의 의미 집중도 유지. 긴 텍스트일수록 벡터가 여러 주제의 평균값으로 수렴하여 검색 정확도 하락 |
| top-20 반환 | 3개 검색 채널(Dense+Sparse+Keyword) × 100개 후보를 전부 반환하면 비효율 |
| results[:3], content[:200] | LLM 입력 토큰 절감. 3개 × 200자 = 600자 vs 10개 × 1000자 = 10,000자 → 비용 16배 차이 |

### 근본적 한계 — 문서형 파이프라인에 표 데이터를 넣은 구조적 미스매치

이 설계는 **매뉴얼, 가이드 같은 문서형 텍스트**에 맞춰진 것:

- 문서형: 1000자 안에 하나의 주제가 담김 → top-5로 충분히 답변 가능
- 표형 (엑셀): 80행이 하나의 플랫 텍스트로 변환됨 → 1000자 안에 여러 고객사가 섞임 → top-K로는 전수 조회 불가

RAG는 "의미 기반 검색"에 최적화되어 있어, "E캐피탈 서버를 빠짐없이 전부 찾아라"는 **전수 조회/집계 질문**에는 구조적으로 한계가 있음.

---

## 4. 수정 사항 정리

### 4.1 엑셀 업로드 실패 수정

#### 원인

프론트엔드 document_type 드롭다운 값(`RESOURCE_STATUS` 등 6개)이 백엔드 Python enum / PostgreSQL enum에 누락.

#### 수정 내용

| 파일 | 변경 |
|------|------|
| `app/models/enums.py` | `DocumentType`에 `WORK_RESULT`, `STARTUP_PROCEDURE`, `SLA`, `RESOURCE_STATUS`, `MONTHLY_REPORT`, `RFP` 추가 |
| PostgreSQL | `ALTER TYPE documenttype ADD VALUE` 6개 실행 |

### 4.2 XLSX/PPTX 추출기 구현

#### 원인

`document_processor.py`의 `_extract_content()`에 XLSX/PPTX 분기가 없어서 fallback text decode 시도 → 실패.

#### 수정 내용

| 파일 | 변경 |
|------|------|
| `app/services/document_processor.py` | `_extract_xlsx()` 추가 — openpyxl(xlsx)/xlrd(xls) 사용. 헤더 자동감지, 합계행 스킵, 시트별 분리, `헤더: 값` 쌍으로 변환 |
| `app/services/document_processor.py` | `_extract_pptx()` 추가 — python-pptx 사용. 슬라이드 텍스트 + 테이블 + 발표자 노트 추출 |
| `app/services/document_processor.py` | `_extract_content()` 라우팅에 XLSX/PPTX MIME 타입 및 확장자 분기 추가 |

### 4.3 RAG 컨텍스트 잘림(truncation) 제거

#### 원인

LLM에 전달되는 검색 결과가 3단계에서 잘려나가서 E캐피탈 서버 16대 중 3대만 LLM에 도달.

#### 수정 내용

| 파일 | 라인 | 변경 전 | 변경 후 |
|------|------|---------|---------|
| `app/services/hybrid_search.py` | 332 | `chunk.content[:500]` | `chunk.content` (전문 전달) |
| `app/services/agentic_rag.py` | 234 | `results["results"][:3]` | `results["results"]` (전체 전달) |
| `app/services/agentic_rag.py` | 239 | `item['content'][:200]` | `item['content']` (전문 전달) |
| `app/services/agentic_rag.py` | 316 | `search_result.results[:5]` | `search_result.results[:10]` (5→10개) |
| `app/services/agentic_rag.py` | 320 | `item.content[:500]` | `item.content` (전문 전달) |
| `app/services/agentic_rag.py` | 331 | `item.content[:500]` | `item.content` (전문 전달) |
| `app/services/agentic_rag.py` | 333 | `search_result.results[:5]` | `search_result.results[:10]` (5→10개) |

#### 검증 결과

| | 수정 전 | 수정 후 |
|---|---|---|
| "E캐피탈 서버 몇 대?" | 3대 (오답) | **16대 (정답)** |
| LLM에 전달되는 청크 수 | 3개 | 10개 |
| 청크당 전달 길이 | 200자 | 전문 (최대 1000자) |

### 4.4 향후 근본적 개선 방향

현재 수정은 truncation 제거로 인한 즉시 효과이며, 구조적 한계는 여전히 존재:

- **엑셀 행 단위 JSONB 구조화 저장**: 필터/집계 질문은 SQL 쿼리로 100% 정확한 답변 가능
- **질문 유형 라우팅**: 설명/검색 → RAG, 필터/집계 → JSONB 쿼리
- **고객사별/시트별 청킹**: 같은 고객사 데이터가 하나의 청크에 모이도록 개선
- **프론트엔드 고객사 선택 UI 추가**: `customerId` 하드코딩 제거

상세 내용은 `개선사항_260526.md` 참조.

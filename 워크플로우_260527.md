# MSP Archive 전체 워크플로우 (2026-05-27)

> 작성일: 2026-05-27
> 목적: 문서 업로드부터 AI 답변 생성까지 전체 파이프라인을 코드 레벨로 추적

---

## 전체 흐름 요약

```
[Phase 1] 업로드          [Phase 2] 처리               [Phase 3] 검색 & 답변

사용자가 파일 업로드      백그라운드 자동 처리           사용자가 질문 입력
     │                        │                            │
     ▼                        ▼                            ▼
 MinIO에 원본 저장        텍스트 추출                  쿼리 임베딩 생성
     │                  (PDF/DOCX/XLSX/PPTX)            (BGE-m3)
     ▼                        │                            │
 Document 레코드 생성         ▼                            ▼
     │                  1000자 단위 청킹              3-Way 병렬 검색
     ▼                  (200자 오버랩)            (Dense+Sparse+Keyword)
 백그라운드 태스크 큐잉       │                            │
                              ▼                            ▼
                        BGE-m3 임베딩 생성           RRF 융합 + 랭킹
                     (Dense 1024d + Sparse 250Kd)         │
                              │                            ▼
                              ▼                    Adaptive RAG 판단
                        DocumentChunk 저장          (simple/complex)
                        (pgvector 컬럼)                    │
                              │                            ▼
                              ▼                    Gemini LLM 답변 생성
                        READY_FOR_SEARCH            (검색 컨텍스트 + 질문)
```

---

## Phase 1: 문서 업로드

### 진입점

- **프론트엔드**: `frontend/src/pages/DocumentPage.tsx` → 업로드 탭
- **API 클라이언트**: `frontend/src/api/archive-client.ts` → `uploadDocument()`
- **백엔드 API**: `POST /api/v1/documents` → `app/api/documents.py:upload_document()`

### 상세 흐름

```
사용자가 프론트엔드에서 파일 선택 + 제목/유형 입력 + 업로드 클릭
     │
     ▼
[archive-client.ts] uploadDocument()
  → FormData 구성: file, title, document_type, customer_id, description, tags
  → POST /api/v1/documents (multipart/form-data)
     │
     ▼
[documents.py] upload_document()
  │
  ├── 1. 파일 내용 읽기
  │      await file.read() → bytes
  │
  ├── 2. SHA-256 해시 계산 (중복 감지용)
  │      hashlib.sha256(content).hexdigest()
  │
  ├── 3. MinIO(S3)에 원본 파일 저장
  │      storage.upload_file()
  │      → 경로: "customers/{customer_id}/original/{uuid8자리}.{확장자}"
  │      → MinIO 컨테이너 (port 9000)에 저장
  │
  ├── 4. Document 레코드 생성 (PostgreSQL)
  │      Document(
  │        title, document_type, customer_id,
  │        file_path, file_name, file_size,
  │        content_hash, mime_type,
  │        processing_status = "UPLOADED",  ← 아직 처리 전
  │        protection_type = "NONE",
  │      )
  │      → db.add() → db.commit()
  │
  └── 5. 백그라운드 처리 태스크 큐잉
         BackgroundTasks.add_task(_process_document_background, document.id)
         → FastAPI BackgroundTask로 비동기 실행
```

### 관련 파일

| 파일 | 역할 |
|------|------|
| `app/api/documents.py` | 업로드 API 엔드포인트 |
| `app/core/storage.py` | MinIO(S3) 파일 저장/다운로드 |
| `app/models/document.py` | Document ORM 모델 |
| `app/models/enums.py` | DocumentType, ProcessingStatus 등 enum 정의 |

---

## Phase 2: 백그라운드 문서 처리

### 진입점

- `app/api/documents.py:_process_document_background()` → `DocumentProcessor.process()`

### 상세 흐름

```
[document_processor.py] DocumentProcessor.process(document_id)
  │
  ├── 1. DB에서 Document 조회
  │
  ├── 2. processing_status → METADATA_EXTRACTED
  │
  ├── 3. 수동 정제본 존재 여부 확인
  │      ManualRefinedDocument 테이블에서 조회
  │      → 있으면 REFINED_UPLOADED 상태로 종료
  │
  ├── 4. S3에서 원본 파일 다운로드
  │      storage.download_file(document.file_path)
  │      → MinIO에서 bytes로 가져옴
  │
  ├── 5. MIME 타입/확장자 기반 텍스트 추출
  │      _extract_content()
  │      │
  │      ├── PDF (.pdf)
  │      │   _extract_pdf() → PyMuPDF(fitz)
  │      │   → 페이지별 page.get_text("text")
  │      │   → "\n\n".join(pages)
  │      │
  │      ├── DOCX (.docx, .doc)
  │      │   _extract_docx() → python-docx
  │      │   → paragraph.text 추출
  │      │
  │      ├── XLSX (.xlsx, .xls)
  │      │   _extract_xlsx() → openpyxl / xlrd
  │      │   → 시트별 분리
  │      │   → 헤더 자동 감지 (non-empty 셀 2개 이상인 첫 행)
  │      │   → 합계/소계 행 스킵
  │      │   → "헤더: 값 | 헤더: 값" 형태로 변환
  │      │   → 예: "서버명: WEB-PRD-01 | IP: 10.1.1.10 | OS: RHEL 8.6"
  │      │
  │      ├── PPTX (.pptx)
  │      │   _extract_pptx() → python-pptx
  │      │   → 슬라이드별 텍스트 + 테이블 + 발표자 노트 추출
  │      │
  │      ├── TXT/MD/CSV (.txt, .md, .csv)
  │      │   _extract_text()
  │      │   → UTF-8/EUC-KR/CP949 자동 감지 디코딩
  │      │
  │      └── 기타
  │          → fallback text decode 시도
  │
  │      텍스트 추출 실패 시:
  │      → processing_status = AWAITING_MANUAL_REFINEMENT
  │      → 여기서 종료
  │
  ├── 6. 청킹 (텍스트 → 청크 분할)
  │      _chunk_content(content)
  │      │
  │      ├── chunk_size = 1000자 (config.py:59 CHUNK_SIZE)
  │      ├── overlap = 200자 (config.py:60 CHUNK_OVERLAP)
  │      │
  │      └── 슬라이딩 윈도우 분할:
  │          for i in range(0, len(content), 800):  # 1000 - 200 = 800 간격
  │              chunk = content[i:i+1000]
  │
  │          예시 (3000자 텍스트):
  │          Chunk 0: [0 ~ 1000]      (1000자)
  │          Chunk 1: [800 ~ 1800]    (1000자, 200자 오버랩)
  │          Chunk 2: [1600 ~ 2600]   (1000자, 200자 오버랩)
  │          Chunk 3: [2400 ~ 3000]   (600자, 나머지)
  │
  └── 7. 임베딩 생성 + DB 저장
         _create_chunks_with_embeddings(document, chunks)
         │
         ├── 배치 처리 (batch_size = 32)
         │
         └── 각 배치에 대해:
             │
             ├── Dense Embedding 요청
             │   POST http://embedding:8080/embed
             │   Body: {"inputs": ["chunk text 1", ...], "truncate": true}
             │   → BGE-m3 모델이 1024차원 벡터 반환
             │   → [0.012, -0.034, 0.089, ..., 0.041]
             │
             ├── Sparse Embedding 요청
             │   POST http://embedding:8080/embed_sparse
             │   Body: {"inputs": ["chunk text 1", ...], "truncate": true}
             │   → [{"index": 1234, "value": 0.56}, ...] 형태 반환
             │   → SPARSEVEC 형식으로 변환: "{1234:0.56,5678:0.34}/250002"
             │
             └── DocumentChunk 레코드 생성 (PostgreSQL)
                 DocumentChunk(
                   document_id, customer_id,
                   document_title,    ← 비정규화 (PGroonga 검색용)
                   content,           ← 청크 텍스트
                   chunk_index,       ← 순서 번호
                   dense_vector,      ← Vector(1024) pgvector 컬럼
                   sparse_vector,     ← SPARSEVEC(250002) pgvector 컬럼
                 )

처리 완료:
  → processing_status = READY_FOR_SEARCH
  → processing_capability = EMBEDDABLE
```

### 관련 파일

| 파일 | 역할 |
|------|------|
| `app/services/document_processor.py` | 텍스트 추출, 청킹, 임베딩 생성 |
| `app/core/config.py` | CHUNK_SIZE(1000), CHUNK_OVERLAP(200) 설정 |
| `app/models/document.py` | DocumentChunk ORM (dense_vector, sparse_vector 컬럼) |

### 상태 전이

```
UPLOADED → METADATA_EXTRACTED → READY_FOR_SEARCH (정상)
                              → AWAITING_MANUAL_REFINEMENT (텍스트 추출 실패)
                              → PROCESSING_FAILED (에러 발생)
```

---

## Phase 3: 검색 & AI 답변 생성

### 3-1. 하이브리드 검색

#### 진입점

- **프론트엔드**: `DocumentPage.tsx` → 검색 탭 → 검색 버튼
- **API**: `POST /api/v1/search/hybrid` → `app/api/hybrid_search.py`
- **서비스**: `app/services/hybrid_search.py:HybridSearchService.search()`

#### 상세 흐름

```
[hybrid_search.py] HybridSearchService.search(request)
  │
  ├── 1. 유효 문서 ID 필터링
  │      _get_valid_document_ids()
  │      → customer_id 기준
  │      → is_active = True
  │      → is_latest = True (버전 관리)
  │      → cross_search=True면 전체 고객사 대상
  │
  ├── 2. 쿼리 임베딩 생성
  │      EmbeddingService.encode(["검색어"])
  │      → POST http://embedding:8080/embed
  │      → POST http://embedding:8080/embed_sparse
  │      → dense_vector (1024d) + sparse_vector (250002d)
  │
  ├── 3. 3-Way 병렬 검색 실행
  │      │
  │      ├── [Dense Search] _vector_search() - pgvector 코사인 거리
  │      │   SELECT id, (dense_vector <=> query_vector::vector) AS score
  │      │   FROM document_chunks
  │      │   WHERE document_id = ANY(valid_ids)
  │      │   ORDER BY score
  │      │   LIMIT 100
  │      │   → score = 1 - cosine_distance (0~1, 높을수록 유사)
  │      │
  │      ├── [Sparse Search] _vector_search() - pgvector sparsevec 거리
  │      │   SELECT id, (sparse_vector <=> query_sparse::sparsevec) AS score
  │      │   FROM document_chunks
  │      │   WHERE document_id = ANY(valid_ids)
  │      │   ORDER BY score
  │      │   LIMIT 100
  │      │
  │      └── [Keyword Search] _keyword_search() - PGroonga 전문검색
  │          SELECT id, pgroonga_score() AS score
  │          FROM document_chunks
  │          WHERE content @@ query_text
  │          ORDER BY score DESC
  │          LIMIT 100
  │
  ├── 4. RRF (Reciprocal Rank Fusion) 융합
  │      _fuse_results()
  │      │
  │      ├── 각 채널에서 chunk의 순위(rank) 추출
  │      │
  │      └── 가중 RRF 계산:
  │          final_score = 0.5/(rank_dense + 60)     ← Dense 가중치 0.5
  │                      + 0.3/(rank_sparse + 60)    ← Sparse 가중치 0.3
  │                      + 0.2/(rank_keyword + 60)   ← Keyword 가중치 0.2
  │
  ├── 5. Dense 스코어 임계값 필터링
  │      _filter_by_threshold()
  │      → dense_score < 0.4인 결과 제외
  │
  ├── 6. Escalation 판단
  │      → 결과 0개 OR 최상위 dense_score < 0.4
  │      → escalation_triggered = True → multi-step 검색으로 전환 트리거
  │
  └── 7. 상위 N개 반환 (기본 limit=20)
         → HybridSearchResponse { results, total, search_mode, escalation_triggered }
```

#### 가중치 상수 (hybrid_search.py)

```python
RRF_K = 60                  # RRF 상수
DENSE_WEIGHT = 0.5          # Dense 검색 가중치
SPARSE_WEIGHT = 0.3         # Sparse 검색 가중치
KEYWORD_WEIGHT = 0.2        # Keyword 검색 가중치
DENSE_SCORE_THRESHOLD = 0.4 # Escalation 임계값
```

### 3-2. Adaptive RAG (AI 답변 생성)

#### 진입점

- **프론트엔드**: `DocumentPage.tsx` → AI 채팅 → 질문 입력
- **API**: `POST /api/v1/search/rag` → `app/api/hybrid_search.py:adaptive_rag()`
- **서비스**: `app/services/agentic_rag.py:AdaptiveRAGOrchestrator.process()`

#### 상세 흐름

```
[agentic_rag.py] AdaptiveRAGOrchestrator.process(query, customer_id)
  │
  ├── 1. 복잡도 분석
  │      agent.analyze_complexity(query)
  │      → Gemini LLM에게 "이 질문이 simple인지 complex인지 판단해" 요청
  │      → "simple": 단일 검색으로 충분 (예: "김 대리 전화번호?")
  │      → "complex": 멀티스텝 필요 (예: "지난 3개월 DB 장애 패턴 비교")
  │
  ├── [Simple Path] ─────────────────────────────────────────────
  │   │
  │   ├── 2a. single_pass_search()
  │   │       → HybridSearchService.search(query, customer_id)
  │   │       → 3-Way 하이브리드 검색 실행 (위 Phase 3-1 전체)
  │   │
  │   ├── 3a. Escalation 체크
  │   │       → escalation_triggered == False → 단일 검색 결과로 답변 생성
  │   │       → escalation_triggered == True → Complex Path로 전환
  │   │
  │   └── 4a. _format_single_pass_response()
  │           │
  │           ├── 검색 결과 상위 10개 선택
  │           │   for item in search_result.results[:10]:
  │           │
  │           ├── 컨텍스트 구성 (청크 전문 포함)
  │           │   "[1] 문서제목"
  │           │   "   섹션: 섹션명"
  │           │   "   청크 전문 텍스트..."
  │           │
  │           └── agent.generate_response() 호출 → 아래 "응답 생성" 참조
  │
  └── [Complex Path] ────────────────────────────────────────────
      │
      ├── 2b. multi_step_search()
      │       │
      │       ├── 쿼리 분해 (_decompose_query)
      │       │   Gemini에게 "이 질문을 검색 친화적 서브쿼리로 분해해" 요청
      │       │   예: "PostgreSQL 장애 대응 절차 알려줘"
      │       │   → ["PostgreSQL 장애 유형", "장애 대응 절차", "복구 명령어"]
      │       │
      │       ├── 각 서브쿼리로 hybrid_search 실행 (최대 5회)
      │       │   for sub_query in query_decomposition:
      │       │       _execute_tool_call(sub_query, customer_id)
      │       │       → HybridSearchService.search()
      │       │
      │       ├── 충분성 판단 (_is_sufficient)
      │       │   → 충분하면 조기 종료
      │       │
      │       └── Fallback: 결과 불충분 시
      │           → cross_search (전체 고객사 대상 검색)
      │
      └── 3b. agent.generate_response()
              → 아래 "응답 생성" 참조

[응답 생성] agent.generate_response(query, tool_results, customer_id)
  │
  ├── 1. 검색 결과 → 컨텍스트 텍스트로 조립
  │      for result in tool_results:
  │          for item in result.results["results"]:
  │              "- {문서제목}"
  │              "  {청크 전문 텍스트}"
  │
  ├── 2. Gemini LLM 호출
  │      messages = [
  │        system: "MSP 운영 전문가 역할, 출처 인용 필수"
  │        user: "검색 결과:\n{context}\n\n질문: {query}\n답변 생성"
  │      ]
  │      → Gemini 2.5 Flash API (OpenAI 호환 형식)
  │      → temperature=0.7, max_tokens=2000
  │
  └── 3. 응답 반환
         → 한국어 답변 + 출처 문서 인용
```

### 관련 파일

| 파일 | 역할 |
|------|------|
| `app/services/hybrid_search.py` | 3-Way 하이브리드 검색 + RRF 융합 |
| `app/services/agentic_rag.py` | Adaptive RAG 오케스트레이터 + Gemini 응답 생성 |
| `app/schemas/search.py` | 검색 요청/응답 스키마 (limit=20 등) |
| `app/core/config.py` | Gemini API 설정, 임베딩 서비스 URL |

---

## 인프라 구성

### Docker Compose 서비스 (8개)

```
┌────────────────────────────────────────────────────────────────┐
│                      Docker Compose (8 services)               │
│                                                                │
│  ┌──────────┐    ┌──────────────┐    ┌──────────────────────┐  │
│  │ Frontend │───▶│    Nginx     │───▶│     FastAPI App      │  │
│  │ React/TS │    │  (port 3000) │    │     (port 8000)      │  │
│  └──────────┘    └──────────────┘    └──────────┬───────────┘  │
│                        /api/*                   │              │
│                       프록시                     │              │
│                                                 │              │
│  ┌──────────┐    ┌──────────────┐    ┌──────────▼───────────┐  │
│  │  Celery  │◀──▶│    Redis     │    │    PostgreSQL 16     │  │
│  │  Worker  │    │  (port 6379) │    │    + pgvector        │  │
│  └────┬─────┘    └──────────────┘    │    + PGroonga        │  │
│       │                              │    (port 5432)       │  │
│       │                              └──────────────────────┘  │
│       │                                                        │
│       │          ┌──────────────┐    ┌──────────────────────┐  │
│       └─────────▶│    MinIO     │    │   HuggingFace TEI    │  │
│                  │ (port 9000)  │    │   BGE-m3 Embedding   │  │
│                  └──────────────┘    │   (port 8080)        │  │
│                                      └──────────────────────┘  │
│                  ┌──────────────┐                               │
│                  │  Gotenberg   │                               │
│                  │ (port 3001)  │                               │
│                  └──────────────┘                               │
└────────────────────────────────────────────────────────────────┘
```

| 서비스 | 역할 | 워크플로우에서의 위치 |
|--------|------|----------------------|
| **Frontend** (React + Nginx) | UI + API 프록시 | Phase 1: 업로드 폼, Phase 3: 검색/채팅 UI |
| **App** (FastAPI) | 백엔드 API + 문서 처리 | Phase 1~3 전체 비즈니스 로직 |
| **PostgreSQL** (+ pgvector + PGroonga) | 메인 DB + 벡터 DB + 전문 검색 | Phase 2: 청크/벡터 저장, Phase 3: 검색 쿼리 |
| **MinIO** (S3 호환) | 원본 파일 저장소 | Phase 1: 파일 저장, Phase 2: 파일 다운로드 |
| **Embedding** (HuggingFace TEI + BGE-m3) | 임베딩 생성 | Phase 2: 청크 임베딩, Phase 3: 쿼리 임베딩 |
| **Redis** | 캐시 + Celery 브로커 | Celery 태스크 큐 (현재는 BackgroundTask 사용) |
| **Celery** | 비동기 문서 처리 워커 | Phase 2: 대안 처리 경로 (현재 미사용) |
| **Gotenberg** | 레거시 문서 변환 | HWP 등 특수 포맷 변환용 (현재 미사용) |

---

## 데이터 흐름 요약

### Phase 1 → Phase 2 연결

```
documents 테이블 (processing_status: UPLOADED)
    → _process_document_background(document_id)
    → S3에서 파일 다운로드
    → 텍스트 추출
    → 청킹
    → BGE-m3 임베딩 생성
    → document_chunks 테이블에 저장 (dense_vector + sparse_vector)
    → documents 테이블 (processing_status: READY_FOR_SEARCH)
```

### Phase 3 데이터 흐름

```
사용자 질문 ("E캐피탈 서버 몇 대?")
    → BGE-m3로 쿼리 벡터 생성
    → PostgreSQL에서 3종 검색:
       ├── pgvector Dense 검색 (코사인 유사도)
       ├── pgvector Sparse 검색 (어휘 유사도)
       └── PGroonga Keyword 검색 (한국어 형태소)
    → RRF 융합으로 최종 순위 결정
    → 상위 10개 청크 텍스트를 Gemini에 전달
    → Gemini가 한국어 답변 생성 + 출처 인용
```

---

## 주요 설정값 (config.py)

| 설정 | 값 | 의미 |
|------|-----|------|
| `CHUNK_SIZE` | 1000 | 청크 크기 (문자 수) |
| `CHUNK_OVERLAP` | 200 | 청크 간 오버랩 (문자 수) |
| `EMBEDDING_DIMENSION` | 1024 | BGE-m3 Dense 벡터 차원 |
| `GEMINI_MODEL` | gemini-2.5-flash | LLM 모델 |
| `AGENT_MAX_TOOL_CALLS` | 5 | Multi-step 최대 검색 횟수 |
| `DENSE_SCORE_THRESHOLD` | 0.4 | Escalation 임계값 |
| `MAX_UPLOAD_SIZE_MB` | 100 | 최대 업로드 크기 |

---

## API 엔드포인트 매핑

| 워크플로우 단계 | HTTP Method | 경로 | 파일 |
|----------------|-------------|------|------|
| 문서 업로드 | POST | `/api/v1/documents` | `app/api/documents.py` |
| 문서 목록 | GET | `/api/v1/documents` | `app/api/documents.py` |
| 하이브리드 검색 | POST | `/api/v1/search/hybrid` | `app/api/hybrid_search.py` |
| Adaptive RAG | POST | `/api/v1/search/rag` | `app/api/hybrid_search.py` |
| 버전 검색 | POST | `/api/v1/search/version` | `app/api/hybrid_search.py` |
| 크로스 검색 | POST | `/api/v1/search/cross` | `app/api/hybrid_search.py` |
| 검색 헬스체크 | GET | `/api/v1/search/health` | `app/api/hybrid_search.py` |
| 앱 헬스체크 | GET | `/health` | `app/main.py` |

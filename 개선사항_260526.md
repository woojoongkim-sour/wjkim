# RAG 파이프라인 고도화 개선사항

> 작성일: 2025-05-26
> 목적: 실제 운영 문서(노이즈 多, 문맥 판단 필요) 기반 Q&A 품질 향상

---

## 1. 현재 상태 진단

### 파이프라인 현황

| 단계 | 현재 구현 | 한계 |
|------|-----------|------|
| 파싱 | PDF(`get_text("text")`), DOCX(paragraph only) | 테이블/구조 무시, PPTX/XLSX 미구현 |
| 청킹 | 1000자 고정 슬라이딩 (overlap 200) | 의미 단위 무시, 메타데이터 소실 |
| 임베딩 | BGE-m3 (dense 1024d + sparse 250k) | 모델 자체는 적절 |
| 검색 | Dense+Sparse+PGroonga RRF | Re-ranking 없음 |
| 생성 | Gemini 2.5 Flash, context 500자 잘림 | 컨텍스트 부족, 프롬프트 단순 |

### DB 통계 (현재)
- 총 67 청크, 평균 890자, max 1000자 (천장 도달)
- 문서당 평균 2~5 청크 → 긴 문서에서 정보 손실 심각
- 실제 PPTX/XLSX 업로드 케이스 없음

---

## 2. 개선 방향 (우선순위순)

### Phase 1: 파싱 강화 (가장 큰 ROI)

**문제**: 실제 운영 문서는 표(연락망, 서버목록, 설정값), 레이아웃(절차서), 슬라이드(교육자료)가 핵심인데 현재 전부 소실됨.

#### 2-1. PDF 파싱 고도화

| 항목 | 현재 | 개선안 |
|------|------|--------|
| 텍스트 | `page.get_text("text")` | `page.get_text("dict")` → 블록/라인 구조 보존 |
| 테이블 | 미지원 | pymupdf `find_tables()` 또는 `camelot`/`pdfplumber` 활용 |
| 페이지 메타 | 없음 | 페이지 번호, 헤더/푸터 제거 로직 추가 |
| OCR 문서 | 기본 텍스트만 | 텍스트 없는 페이지 감지 → OCR fallback (Tesseract/EasyOCR) |
| 노이즈 제거 | 없음 | 반복 헤더/푸터, 워터마크, 페이지번호 패턴 필터링 |

#### 2-2. PPTX 추출기 구현 (현재 미구현)

```python
# 구현 방향
- 슬라이드별 텍스트 + 노트(발표자 노트) 추출
- 테이블/차트 텍스트 추출
- 슬라이드 번호 메타데이터 보존
- 그룹화된 텍스트박스 순서 보존
```

#### 2-3. XLSX/XLS 추출기 구현 (현재 미구현)

```python
# 구현 방향
- 시트별 분리 처리
- 테이블 구조를 Markdown 테이블로 변환 (LLM이 잘 이해함)
- 빈 행/열 필터링
- 병합 셀 처리
- 시트명 메타데이터 보존
```

#### 2-4. DOCX 파싱 강화

```python
# 현재: paragraphs만 추출
# 개선:
- 테이블 → Markdown 변환
- 헤딩 레벨(h1/h2/h3) 감지 → 섹션 구조 보존
- 리스트(bullet/numbered) 구조 보존
- 이미지 캡션 추출
```

---

### Phase 2: 청킹 고도화

**문제**: 1000자 고정 슬라이딩은 "서버 장애 대응 절차"가 1~3단계는 chunk A, 4~6단계는 chunk B로 잘려서 LLM이 전체 절차를 답변 못함.

#### 개선안: Semantic + Structure-aware Chunking

```
전략:
1. 1차 분할: 구조 기반 (헤딩, 섹션 경계, 테이블 단위)
2. 2차 분할: 크기 초과 시 문장 단위로 재분할
3. 메타데이터 보존: 각 청크에 (페이지, 섹션명, 문서제목, 부모섹션) 태깅
```

| 파라미터 | 현재 | 권장 |
|----------|------|------|
| chunk_size | 1000자 | 1500~2000자 (BGE-m3 max 8192 토큰이므로 여유) |
| overlap | 200자 | 문장 단위 1~2문장 overlap |
| 분할 기준 | 문자 수 | 섹션 > 단락 > 문장 (계층적) |
| 메타데이터 | 없음 | page_num, section_title, parent_section, chunk_type(text/table/list) |

#### 테이블 청킹 전략

```
- 테이블은 하나의 청크로 유지 (잘리면 의미 소실)
- 대형 테이블(50행+)은 헤더를 각 청크에 반복 포함
- 테이블 앞뒤 1문장 컨텍스트 포함 ("아래는 비상연락망입니다")
```

#### Contextual Chunking (Anthropic 방식)

```
각 청크에 문서 전체 맥락 요약을 prefix로 추가:
"[문서: 서버_장애_대응_매뉴얼.pdf | 섹션: 3. 장애 등급별 대응] ..."
→ 임베딩 품질 + 검색 정확도 동시 향상
```

---

### Phase 3: 검색 개선 (Re-ranking 추가)

**문제**: RRF 퓨전만으로는 노이즈 문서에서 진짜 관련 청크를 가려내기 어려움. 특히 유사한 키워드가 여러 문서에 존재할 때.

#### Re-ranker 도입

```
검색 흐름 (개선 후):
Query → Hybrid Search (top 100) → Re-ranker (top 10~20) → LLM Context
```

| Re-ranker 옵션 | 장점 | 단점 |
|----------------|------|------|
| **BGE-reranker-v2-m3** | 로컬 실행(TEI), 한국어 OK, 무료 | GPU 필요, 속도 |
| **Cohere Rerank v3** | API 호출, 매우 정확, 다국어 | 유료 ($1/1k queries) |
| **Jina Reranker v2** | API, 가성비 | Cohere 대비 약간 열세 |

**권장**: BGE-reranker-v2-m3 (현재 TEI 인프라 재활용 가능)

---

### Phase 4: 생성(Generation) 개선

#### 4-1. 컨텍스트 확장

| 항목 | 현재 | 개선안 |
|------|------|--------|
| 청크 전달량 | content[:500] | 전문 전달 (re-rank 후 top 5~10) |
| 최대 토큰 | 2000 | 4000~8000 |
| 메타데이터 | 없음 | 출처(문서명, 페이지, 섹션) 포함 |

#### 4-2. 프롬프트 엔지니어링

```
개선 방향:
- 문서 유형별 프롬프트 분기 (절차서 vs 연락망 vs 설정가이드)
- Chain-of-Thought: "먼저 관련 문서를 확인하고, 해당 섹션에서 답변을 구성하세요"
- Citation 강제: "[문서명, p.X] 형식으로 반드시 출처 명시"
- 답변 불가 시 명확히 표현: "해당 정보는 업로드된 문서에서 찾을 수 없습니다"
```

#### 4-3. 컨텍스트 압축 (긴 문서 대응)

```
- Re-ranked 결과가 많을 때: LLM에 한번에 다 넘기지 않고
  1) 각 청크를 요약 (map)
  2) 요약 합쳐서 최종 답변 (reduce)
- 또는: Lost-in-the-Middle 방지를 위해 관련도순 배치
```

---

## 3. 모델 추천

### 임베딩 모델

| 모델 | 특징 | 추천도 |
|------|------|--------|
| **BGE-m3 (현재)** | Dense+Sparse 동시 지원, 한국어 OK, 로컬 실행 | ⭐⭐⭐⭐ 유지 권장 |
| multilingual-e5-large-instruct | Instruction-tuned, MS 제작 | ⭐⭐⭐ 대안 |
| Cohere embed-v3 | API, 다국어 최상위, 노이즈에 강함 | ⭐⭐⭐⭐ (비용 여유 시) |

**결론**: BGE-m3 유지 + Re-ranker 추가가 가성비 최선. 임베딩 모델 교체보다 Re-ranker가 ROI 훨씬 높음.

### LLM (생성) 모델

| 모델 | 한국어 | 긴 컨텍스트 | 추론력 | 비용 | 추천 용도 |
|------|--------|-------------|--------|------|-----------|
| **Gemini 2.5 Flash (현재)** | ⭐⭐⭐ | 1M tokens | ⭐⭐⭐ | 저렴 | 단순 Q&A 유지 |
| **Gemini 2.5 Pro** | ⭐⭐⭐⭐ | 1M tokens | ⭐⭐⭐⭐⭐ | 중간 | 복잡한 문맥 추론 필요 시 |
| Claude 4 Sonnet | ⭐⭐⭐⭐⭐ | 200k | ⭐⭐⭐⭐⭐ | 중간 | 한국어 뉘앙스, 절차 이해 |
| GPT-4o | ⭐⭐⭐⭐ | 128k | ⭐⭐⭐⭐ | 중간 | 범용 |
| GPT-4o-mini | ⭐⭐⭐ | 128k | ⭐⭐⭐ | 저렴 | Flash 대안 |

**권장 전략**:
```
- 단순 쿼리 (single-pass): Gemini 2.5 Flash 유지 (비용 효율)
- 복잡 쿼리 (multi-step): Gemini 2.5 Pro로 업그레이드
- 향후 한국어 품질 이슈 시: Claude Sonnet 테스트
```

**이유**: 
- 노이즈 많은 실제 문서에서는 LLM의 "문맥 추론력"이 핵심
- Flash는 속도/비용 OK지만, "A 문서 3페이지와 B 문서 7페이지를 종합하면..." 같은 복합 추론에 약함
- Pro는 같은 Gemini 생태계라 마이그레이션 비용 0 (model명만 변경)

### Re-ranker 모델

| 모델 | 배포 | 한국어 | 추천도 |
|------|------|--------|--------|
| **BGE-reranker-v2-m3** | 로컬 (TEI) | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ (인프라 재활용) |
| Cohere Rerank v3.5 | API | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ (정확도 최고) |
| Jina Reranker v2 | API | ⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## 4. 실제 문서 대응 전략

### 노이즈 유형별 대응

| 노이즈 유형 | 예시 | 대응 방안 |
|-------------|------|-----------|
| 반복 헤더/푸터 | "㈜XX회사 대외비" 매 페이지 | 파싱 시 패턴 감지 → 제거 |
| 페이지 번호 | "- 3 -", "3/15" | 정규식 필터링 |
| 빈 페이지/장식 | 표지, 목차 | 텍스트 밀도 기반 필터링 (< 50자 무시) |
| OCR 오류 | "서버" → "서버", 깨진 글자 | 후처리 교정 또는 LLM 기반 정제 |
| 암호화/DRM | 한글(hwp) DRM | Gotenberg 변환 또는 수동 정제 플로우 유지 |

### 문맥 판단이 필요한 케이스 대응

| 케이스 | 예시 | 대응 |
|--------|------|------|
| 참조 관계 | "위 표 참조", "3장 참고" | 섹션 구조 보존 + parent chunk 연결 |
| 암시적 주어 | "해당 서버는..." (어떤 서버?) | 청크에 문서 컨텍스트(고객명, 서비스) prefix |
| 절차의 연속성 | 1단계→5단계 걸쳐있는 답변 | 큰 chunk size + 인접 청크 확장 검색 |
| 표+텍스트 혼합 | "아래 표의 IP로 접속" | 테이블 청크에 전후 텍스트 포함 |

---

## 5. 구현 우선순위 로드맵

```
[P0 - 즉시 효과] (1~2일)
├── CHUNK_SIZE 1000 → 2000 확대
├── 생성 시 content[:500] 제한 제거 → 전문 전달
├── Gemini Flash → Pro 모델 분기 (복잡 쿼리만)
└── PPTX/XLSX 기본 추출기 구현

[P1 - 핵심 개선] (3~5일)
├── PDF 테이블 추출 (pymupdf find_tables)
├── DOCX 테이블/헤딩 구조 추출
├── Semantic chunking (섹션 경계 기반)
├── 청크 메타데이터 (page_num, section_title) 추가
└── 노이즈 필터링 (헤더/푸터/페이지번호)

[P2 - 품질 도약] (1주)
├── Re-ranker 도입 (BGE-reranker-v2-m3)
├── Contextual chunking (문서 맥락 prefix)
├── 인접 청크 확장 검색 (window expansion)
├── 프롬프트 엔지니어링 고도화
└── 답변 품질 평가 파이프라인 구축

[P3 - 고급] (2주+)
├── OCR fallback (이미지 PDF 대응)
├── Map-Reduce 생성 (대량 컨텍스트)
├── 문서 간 관계 그래프 활용
└── 사용자 피드백 기반 파인튜닝 데이터 수집
```

---

## 6. 즉시 적용 가능한 Quick Win

현재 코드에서 설정값만 변경해도 체감 개선되는 항목:

```python
# app/core/config.py
CHUNK_SIZE: int = 2000          # 현재 1000 → 2000
CHUNK_OVERLAP: int = 300        # 현재 200 → 300

# app/services/agentic_rag.py - generate_response()
# content[:200] → content[:1500] 또는 전문
# max_tokens: 2000 → 4000

# app/core/config.py
GEMINI_MODEL: str = "gemini-2.5-pro"  # 복잡 쿼리 시
```

---

## 7. 평가 방안

개선 효과를 측정하기 위한 평가 체계:

```
1. Golden QA Set 구축
   - 실제 운영 문서 10~20개 업로드
   - 문서별 예상 질문-정답 쌍 50개+ 작성
   - 난이도 분류: 단순(직접 언급) / 추론(문맥) / 복합(다문서)

2. 자동 평가 메트릭
   - Retrieval: Recall@5, MRR (정답 청크가 top5에 있는가)
   - Generation: LLM-as-judge (정확성, 완전성, 출처 인용)
   - Latency: 응답 시간 P50/P95

3. A/B 비교
   - 개선 전/후 동일 질문셋으로 비교
   - 파이프라인 단계별 ablation (어떤 개선이 얼마나 효과적인지)
```

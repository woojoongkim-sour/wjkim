# RAG 파이프라인 고도화 개선사항

> 작성일: 2025-05-26  
> 목적: 실제 운영 문서(노이즈 多, 문맥 판단 필요) 기반 Q&A 품질 향상  
> 상태: 방향 수립 완료 (구현 미착수)

---

## 1. 현재 상태 진단

### 파이프라인 현황

| 단계 | 현재 구현 | 한계 |
|------|-----------|------|
| 파싱 | PDF(`get_text("text")`), DOCX(paragraph only) | 테이블/구조 무시, PPTX/XLSX 미구현 |
| 청킹 | 1000자 고정 슬라이딩 (overlap 200) | 의미 단위 무시, 메타데이터 소실, 천장 도달 |
| 임베딩 | BGE-m3 (dense 1024d + sparse 250k) | 모델 자체는 적절 |
| 검색 | Dense+Sparse+PGroonga RRF | Re-ranking 없음 |
| 생성 | Gemini 2.5 Flash, context 500자 잘림 | 컨텍스트 부족, 프롬프트 단순 |

### DB 통계 (2025-05-26 기준)
- 총 67 청크, 평균 890자, max 1000자 (천장 도달)
- 문서당 평균 2~5 청크 → 긴 문서에서 정보 손실 심각
- 실제 PPTX/XLSX 업로드 케이스 없음
- 테스트 문서가 단순하여 현재 품질 측정이 무의미

---

## 2. 핵심 개선 영역

### 2.1 파싱 강화 (가장 큰 ROI)

**문제**: 실제 운영 문서는 표(연락망, 서버목록, 설정값), 레이아웃(절차서), 슬라이드(교육자료)가 핵심인데 현재 전부 소실.

#### PDF 파싱 고도화

| 항목 | 현재 | 개선안 |
|------|------|--------|
| 텍스트 | `page.get_text("text")` | `page.get_text("dict")` → 블록/라인 구조 보존 |
| 테이블 | 미지원 | pymupdf `find_tables()` 또는 `pdfplumber` 활용 |
| 페이지 메타 | 없음 | 페이지 번호, 헤더/푸터 제거 로직 추가 |
| OCR 문서 | 기본 텍스트만 | 텍스트 없는 페이지 감지 → OCR fallback |
| 노이즈 제거 | 없음 | 반복 헤더/푸터, 워터마크, 페이지번호 패턴 필터링 |

#### PPTX 추출기 (현재 미구현)
- 슬라이드별 텍스트 + 노트(발표자 노트) 추출
- 테이블/차트 텍스트 추출
- 슬라이드 번호 메타데이터 보존

#### XLSX/XLS 추출기 (현재 미구현)
- 시트별 분리 처리
- 테이블 구조를 Markdown 테이블 + 행 단위 자연어로 이중 변환
- 빈 행/열 필터링, 병합 셀 처리
- 시트명 메타데이터 보존
- 헤더 행 자동 감지 (1행이 아닐 수 있음)

#### DOCX 파싱 강화
- 테이블 → Markdown 변환
- 헤딩 레벨(h1/h2/h3) 감지 → 섹션 구조 보존
- 리스트(bullet/numbered) 구조 보존

---

### 2.2 청킹 고도화

**문제**: 1000자 고정 슬라이딩은 절차서 단계가 잘리고, 표 데이터가 분리되고, "이 내용이 어떤 문서/섹션인지" 정보가 소실됨.

#### 왜 청크 사이즈를 단순히 키우면 안 되는가

| 장점 (사이즈 증가) | 단점 (사이즈 증가) |
|---|---|
| 문맥 보존 | 임베딩 희석 — 2000자 안에 주제 2~3개 섞이면 벡터가 평균값으로 수렴 |
| LLM 맥락 파악 쉬움 | 검색 정밀도 하락 (노이즈 섞인 청크가 매칭됨) |
| 청크 수 감소 | top-K에 중복 정보 늘어남 |

예시:
```
작은 청크 (500자): "김철수 연락처: 010-1234-5678" → 임베딩이 정확히 이걸 표현
큰 청크 (2000자): "김철수 연락처 + 서버 IP목록 + 대응절차 일부" → 벡터가 뭘 표현하는지 모호
```

#### 개선안: 구조 기반 가변 청킹 + Small-to-Big

**Step 1: 파싱 시 구조화된 중간 표현(IR) 생성**

```
Document
├── Section("1. 개요", level=1)
│   ├── Paragraph("본 문서는...")
│   └── Paragraph("대상 시스템은...")
├── Section("2. 비상연락망", level=1)
│   ├── Paragraph("아래는 담당자 연락처입니다")
│   └── Table(rows=15, headers=["이름","직책","연락처"])
├── Section("3. 장애대응 절차", level=1)
│   ├── Section("3.1 1등급 장애", level=2)
│   │   ├── Paragraph("정의: 서비스 전면 중단...")
│   │   └── List(["즉시 보고", "핫라인 연결", "전원 투입"])
```

**Step 2: 트리 순회 기반 가변 청크 생성**

규칙:
1. 리프 노드(paragraph, table, list)를 순서대로 누적
2. 누적 크기가 max_chunk_size(1500자) 넘으면 → 이전까지를 하나의 청크로 확정
3. 경계 규칙:
   - 섹션(heading) 바뀌면 → 무조건 새 청크 시작
   - 테이블 → 통째로 하나의 청크 (크기 무관)
   - 리스트 → 가능하면 안 자름
4. 각 청크에 메타데이터 태깅: {section_path, page_num, chunk_type}

**Step 3: Small-to-Big 패턴**

```
저장 구조:
┌─────────────────────────────────┐
│ parent_chunk (섹션 전체, ~3000자) │  ← LLM에 전달용
│  ├── child_chunk_1 (500자)       │  ← 검색 매칭용 (임베딩 대상)
│  ├── child_chunk_2 (700자)       │
│  └── child_chunk_3 (400자)       │
└─────────────────────────────────┘

검색: child_chunk로 정밀 매칭 (벡터 유사도)
생성: 매칭된 child의 parent_chunk를 LLM에 전달 (풍부한 문맥)
```

DB 변경: `document_chunks` 테이블에 `parent_chunk_id` 컬럼 추가

#### Contextual Chunking (Anthropic 방식)

각 청크에 문서 전체 맥락 요약을 prefix로 추가:
```
"[문서: 서버_장애_대응_매뉴얼.pdf | 섹션: 3. 장애 등급별 대응] ..."
```
→ 임베딩 품질 + 검색 정확도 동시 향상

---

### 2.3 엑셀 전용 전략 (자원관리대장 등)

#### 왜 엑셀은 일반 RAG로 한계가 있는가

자원관리대장(n백 대 서버)에서의 질문 유형:

| 질문 유형 | 예시 | RAG만으로 | JSONB 쿼리 |
|-----------|------|-----------|------------|
| 단일 lookup | "DB-01 메모리?" | ✅ | ✅ |
| 필터링 | "김철수 담당 서버 전부?" | ⚠️ top-K에 안 걸리면 누락 | ✅ 전수 |
| 집계 | "메모리 합계?" | ❌ LLM 계산 부정확 | ✅ SQL |
| 비정형 | "DB-01 장애 시 대응방법?" | ✅ 관련 문서 종합 | ❌ 테이블에 없음 |

**문제**: 고객사 합산 n백 대 규모면, "김철수 담당 서버 전부"를 RAG top-K로는 빠짐없이 못 뽑음.

#### 선택한 전략: Level 2 — JSONB 병행 저장

```sql
CREATE TABLE structured_records (
    id SERIAL PRIMARY KEY,
    document_id INT REFERENCES documents(id),
    customer_id INT,
    sheet_name TEXT,
    row_index INT,
    data JSONB,  -- {"서버명": "DB-01", "IP": "192.168.1.20", "메모리": "64GB", ...}
    created_at TIMESTAMP DEFAULT NOW()
);

-- 필터링 질문:
SELECT data FROM structured_records
WHERE customer_id = 3 AND data->>'담당자' = '김철수';

-- 집계 질문:
SELECT COUNT(*) FROM structured_records
WHERE data->>'OS' LIKE '%RHEL 8%';
```

**장점**: 
- 고객사마다 컬럼명/수가 달라도 JSONB로 수용
- 기존 RAG 파이프라인 건드리지 않고 레이어 추가
- LLM 역할: 질문 → JSONB 쿼리 조건 생성만

**라우팅 흐름**:
```
질문 들어옴
  → LLM이 질문 유형 판단
     ├── "정보 조회/설명/비정형" → 기존 RAG 파이프라인
     └── "필터/집계/목록" → JSONB 쿼리 → 정확한 결과 → LLM 포맷팅
```

#### 엑셀 파싱 시 이중 저장

```
엑셀 업로드:
  1. 행 단위 자연어 변환 → document_chunks (벡터 검색용)
  2. 원본 행 데이터 → structured_records JSONB (정확 쿼리용)
```

#### 엑셀 노이즈 대응

| 노이즈 | 예시 | 대응 |
|--------|------|------|
| 제목/회사정보 행 | "㈜ABC 자산관리대장 (대외비)" | 데이터 영역 자동 감지, 비데이터 행 스킵 |
| 병합 셀 | 구분 그룹핑 | unmerge 후 값 복제 |
| 빈 행 | 구분선 역할 | 연속 빈 행으로 데이터 블록 분리 |
| 합계 행 | "총 80건", "=SUM(...)" | 패턴 감지 제외 ("합계", "소계", "Total") |
| 불규칙 스펙 표기 | "8C/32G" vs "8코어/32기가" | 있는 그대로 저장 (LLM이 이해) |
| 헤더가 1행이 아님 | 3~4행에 실제 헤더 | 데이터 패턴 분석으로 헤더 행 추정 |

---

### 2.4 검색 개선 (Re-ranking)

**문제**: RRF 퓨전만으로는 노이즈 문서에서 진짜 관련 청크를 가려내기 어려움.

```
개선 후 검색 흐름:
Query → Hybrid Search (top 100) → Re-ranker (top 10~20) → LLM Context
```

| Re-ranker 옵션 | 장점 | 단점 |
|----------------|------|------|
| **BGE-reranker-v2-m3** | 로컬(TEI 재활용), 한국어 OK, 무료 | GPU 없으면 느림 |
| Cohere Rerank v3.5 | API, 매우 정확, 다국어 | 유료 |
| Jina Reranker v2 | API, 가성비 | Cohere 대비 약간 열세 |

**권장**: BGE-reranker-v2-m3 (현재 TEI 인프라 재활용 가능)

---

### 2.5 생성(Generation) 개선

#### 컨텍스트 확장

| 항목 | 현재 | 개선안 |
|------|------|--------|
| 청크 전달량 | content[:500] 잘림 | 전문 전달 (re-rank 후 top 5~10) |
| 최대 토큰 | 2000 | 4000~8000 |
| 메타데이터 | 없음 | 출처(문서명, 페이지, 섹션) 포함 |

#### 프롬프트 엔지니어링
- Chain-of-Thought: "먼저 관련 문서를 확인하고, 해당 섹션에서 답변 구성"
- Citation 강제: "[문서명, p.X] 형식으로 출처 명시"
- 답변 불가 시 명확히: "해당 정보는 업로드된 문서에서 찾을 수 없습니다"

#### 컨텍스트 압축 (긴 문서 대응)
- Lost-in-the-Middle 방지: 관련도순 배치
- Map-Reduce: 청크 많을 때 각각 요약 후 합산

---

## 3. 모델 추천

### 임베딩 모델

| 모델 | 특징 | 판단 |
|------|------|------|
| **BGE-m3 (현재)** | Dense+Sparse 동시, 한국어 OK, 로컬 | ✅ 유지. 임베딩 교체보다 Re-ranker 추가가 ROI 높음 |
| Cohere embed-v3 | API, 노이즈에 강함 | 비용 여유 시 대안 |

### LLM (생성) 모델

| 모델 | 한국어 | 컨텍스트 | 추론력 | 비용 | 용도 |
|------|--------|----------|--------|------|------|
| **Gemini 2.5 Flash (현재)** | ⭐⭐⭐ | 1M | ⭐⭐⭐ | 저렴 | 단순 Q&A 유지 |
| **Gemini 2.5 Pro** | ⭐⭐⭐⭐ | 1M | ⭐⭐⭐⭐⭐ | 중간 | 복잡 문맥 추론 |
| Claude Sonnet | ⭐⭐⭐⭐⭐ | 200k | ⭐⭐⭐⭐⭐ | 중간 | 한국어 뉘앙스 |
| GPT-4o | ⭐⭐⭐⭐ | 128k | ⭐⭐⭐⭐ | 중간 | 범용 |

**권장 전략**:
- 단순 쿼리 (single-pass): Flash 유지
- 복잡 쿼리 (multi-step): Pro 업그레이드 (model명만 변경, 마이그레이션 비용 0)
- 노이즈 많은 문맥 추론: Pro가 Flash 대비 확실히 우위

### Re-ranker 모델

| 모델 | 배포 | 추천도 |
|------|------|--------|
| **BGE-reranker-v2-m3** | 로컬 (TEI) | ⭐⭐⭐⭐⭐ (인프라 재활용) |
| Cohere Rerank v3.5 | API | ⭐⭐⭐⭐ (정확도 최고) |

---

## 4. 구현 우선순위 로드맵

```
[P0 - 즉시 효과] (1~2일)
├── 생성 시 content[:500] 제한 제거 → 전문 전달
├── Gemini Flash → Pro 모델 분기 (복잡 쿼리만)
├── PPTX/XLSX 기본 추출기 구현
└── 엑셀 행 단위 자연어 변환 청킹

[P1 - 핵심 개선] (3~5일)
├── PDF 테이블 추출 (pymupdf find_tables)
├── DOCX 테이블/헤딩 구조 추출
├── 구조 기반 가변 청킹 (섹션 경계 기반)
├── 청크 메타데이터 (page_num, section_title) 추가
├── 노이즈 필터링 (헤더/푸터/페이지번호)
└── structured_records JSONB 테이블 + 엑셀 이중 저장

[P2 - 품질 도약] (1주)
├── Re-ranker 도입 (BGE-reranker-v2-m3)
├── Small-to-Big (parent/child chunk) 구현
├── Contextual chunking (문서 맥락 prefix)
├── 질문 유형 라우팅 (RAG vs JSONB 쿼리)
├── 프롬프트 엔지니어링 고도화
└── 답변 품질 평가 파이프라인 구축

[P3 - 고급] (2주+)
├── OCR fallback (이미지 PDF 대응)
├── Map-Reduce 생성 (대량 컨텍스트)
├── 문서 간 관계 그래프 활용
└── 사용자 피드백 기반 개선 루프
```

---

## 5. 테스트 계획

### 5.1 테스트 파일 (samples/excel_test/)

| 파일 | 특성 | 테스트 목적 |
|------|------|-------------|
| `01_자원관리대장_정상.xlsx` | 깔끔한 3시트(서버/네트워크/라이선스) | 기본 추출 검증 |
| `02_인프라현황_B고객사_정상.xlsx` | 다른 컬럼명/구조 | 고객사별 스키마 차이 대응 |
| `03_IT자산관리대장_노이즈_병합셀.xlsx` | 제목행, 병합셀, 합계행, 빈행 | 노이즈 필터링 검증 |
| `04_서버현황_멀티시트_메모포함.xlsx` | PRD/DR서버 + 비정형 운영메모 | 정형+비정형 혼합 처리 |
| `05_통합서버현황_80대_불규칙.xlsx` | 80행, 스펙 포맷 불일치, 빈 셀 | 대량 데이터 + 불규칙 포맷 |
| `06_월간운영보고서_보고서형.xlsx` | 보고서(텍스트+표 혼합) | 엑셀이지만 문서형 처리 |

### 5.2 프론트엔드 AI 채팅 테스트 시나리오

#### 난이도별 질문 세트

**Level 1: 단순 lookup (정답이 한 셀에 있음)**
```
- "WEB-PRD-01 IP 주소 알려줘"
- "DB-PRD-01 담당자 누구야?"
- "코어스위치 모델명이 뭐야?"
- "Oracle 라이선스 만료일 언제야?"
```

**Level 2: 필터링 (여러 행 대상)**
```
- "김철수가 담당하는 서버 목록 보여줘"
- "RHEL 8.6 운영체제 사용하는 서버 다 알려줘"
- "메모리 64GB 이상 서버는?"
- "2022년 3월에 설치된 서버들은?"
```

**Level 3: 집계/비교 (계산 필요)**
```
- "전체 서버 몇 대야?"
- "담당자별 서버 수 정리해줘"
- "월과금 합계 얼마야?"
- "가장 오래된 서버는?"
```

**Level 4: 크로스시트/크로스문서 (여러 소스 종합)**
```
- "DR 전환 시 RTO가 가장 긴 서비스는?"
- "prd-erp-db에 대한 운영 이슈 있었어?"
- "이DBA가 담당하는 서버 중 메모리 이슈가 있는 건?"
- "12월에 예정된 작업 중 ERP 관련된 것은?"
```

**Level 5: 문맥 추론 (직접 명시 안 됨)**
```
- "Oracle DB 관련해서 최근 이슈랑 조치사항 정리해줘"
- "메일서버 보안 상태 괜찮아?"  (인증서 만료/갱신 내역에서 추론)
- "ERP 시스템 안정성 어때?"  (메모리 85%, 패치 예정 등 종합)
- "서버 증설이 필요한 것들 있어?"
```

#### 테스트 평가 기준

| 기준 | 설명 | 측정 방법 |
|------|------|-----------|
| **정확성** | 답변 내용이 문서 데이터와 일치하는가 | 수동 검증 (정답 대조) |
| **완전성** | 빠진 정보 없이 전부 답변했는가 | 특히 필터링 질문에서 누락 체크 |
| **출처 인용** | 어떤 문서/시트에서 가져왔는지 명시하는가 | citation 포함 여부 |
| **불가 판단** | 없는 정보를 묻을 때 "모른다"고 하는가 | 환각(hallucination) 체크 |
| **응답 시간** | 사용자가 기다릴 수 있는 범위인가 | < 10초 목표 |

#### 테스트 진행 방법

```
1. 테스트 파일 6개를 프론트엔드에서 각 고객사에 업로드
2. 처리 완료 확인 (READY_FOR_SEARCH 상태)
3. AI 채팅에서 Level 1~5 질문 순차 실행
4. 각 답변을 [정확/부분정확/오답/환각/무응답]으로 분류
5. 개선 전/후 동일 질문셋 비교 → 어떤 개선이 효과적이었는지 측정
```

#### 개선 전 기대 결과 (현재 상태 예측)

```
Level 1 (단순 lookup):     60~70% 정확  ← 청크에 걸리면 OK
Level 2 (필터링):          30~40% 완전  ← top-K에 안 걸리는 행 존재
Level 3 (집계):            10~20% 정확  ← LLM 계산 부정확
Level 4 (크로스):          20~30% 정확  ← 여러 소스 종합 어려움
Level 5 (추론):            30~40% 정확  ← 문맥 부족
```

#### 개선 후 목표

```
Level 1: 95%+
Level 2: 90%+ (JSONB 쿼리 도입 시)
Level 3: 85%+ (JSONB 집계 쿼리 도입 시)
Level 4: 70%+
Level 5: 60%+
```

---

## 6. 노이즈 대응 전략 상세

### 실제 운영 문서 노이즈 유형

| 노이즈 유형 | 예시 | 대응 |
|-------------|------|------|
| 반복 헤더/푸터 | "㈜XX회사 대외비" 매 페이지 | 파싱 시 패턴 감지 → 제거 |
| 페이지 번호 | "- 3 -", "3/15" | 정규식 필터링 |
| 제목/장식 행 (엑셀) | "[ 통합 서버 현황표 ]" | 데이터 영역 자동 감지 |
| 합계/소계 행 | "총 80건", "=SUM(...)" | 패턴 감지 제외 |
| OCR 오류 | "서버" → "서버", 깨진 글자 | 후처리 교정 |
| 암호화/DRM | 한글(hwp) DRM | Gotenberg 변환 또는 수동 정제 |
| 불규칙 포맷 | "8C/32G" vs "8코어/32기가" | 있는 그대로 저장 (LLM이 이해) |

### 문맥 판단이 필요한 케이스

| 케이스 | 대응 |
|--------|------|
| 참조 관계 ("위 표 참조") | 섹션 구조 보존 + parent chunk 연결 |
| 암시적 주어 ("해당 서버는...") | 청크에 문서 컨텍스트 prefix |
| 절차의 연속성 (1→5단계) | 구조 기반 청킹 + 인접 청크 확장 |
| 표+텍스트 혼합 | 테이블 청크에 전후 텍스트 포함 |
